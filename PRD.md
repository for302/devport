# DevPort Manager V2 - 통합 개발 환경 관리자

## 비전
**XAMPP를 대체하는 차세대 통합 개발 환경** - 설치, 서비스 관리, 프로젝트 관리, DB 관리까지 올인원

---

## 핵심 기능 요약

| 기능 | 설명 |
|------|------|
| 통합 설치 | Apache, MariaDB, PHP, Node.js, Git 번들 (Python 제외) |
| 서비스 컨트롤 | 시작/정지/자동재시작, 로그 뷰어, 명령 편집 |
| 프로젝트 관리 | 템플릿으로 생성, 포트/도메인 설정, 원클릭 열기 |
| DB 관리 | phpMyAdmin 통합 (`/phpmyadmin`), 프로젝트별 덤프/복원 |
| 환경변수 | .env GUI 편집기, 프로필(dev/staging) |
| 보안 정책 | localhost 바인딩, 프로젝트별 DB 계정 |

---

## 운영 모드 (Operating Mode)

### 프로세스 방식 채택 (MVP)
DevPort는 **프로세스 방식**으로 운영 (Windows 서비스 등록 없음):

| 항목 | 설정 |
|------|------|
| 설치 방식 | NSIS 인스톨러 (관리자 권한으로 설치) |
| 설치 경로 | `C:\DevPort\` 고정 (MVP), 향후 선택 옵션 제공 |
| 권한 | 설치 시 1회 관리자 권한, 이후 일반 사용자로 실행 |
| 실행 방식 | DevPort 앱이 직접 프로세스 관리 |
| 자동 시작 | Task Scheduler 등록 (옵션) |
| 종료 방식 | Quit(앱 종료) 시 모든 서비스 함께 종료 (X=트레이 최소화는 서비스 유지) |

**프로세스 방식 선택 이유:**
- 개발 환경은 필요할 때만 실행
- Windows 서비스 복잡성 회피
- 디버깅 용이 (콘솔 출력 직접 확인)

### 창 닫기 vs 종료 (UX 정책)

| 액션 | 동작 | 서비스 상태 |
|------|------|-------------|
| **창 닫기 (X)** | 시스템 트레이로 최소화 | 서비스 계속 실행 |
| **종료 (Quit)** | DevPort 완전 종료 | 모든 서비스 함께 종료 |

```
┌─────────────────────────────────────────────────────────────────┐
│  DevPort is running in the background                          │
│                                                                 │
│  🟢 Apache (8080)  🟢 MariaDB (3306)                           │
│                                                                 │
│  [열기]  [종료]                                   <- 트레이 메뉴│
└─────────────────────────────────────────────────────────────────┘
```

**트레이 동작:**
- 창 닫기(X) 클릭 시 트레이로 최소화 (서비스 유지)
- 트레이 아이콘 더블클릭 → 메인 창 열기
- 트레이 메뉴 → "종료" 클릭 시 모든 서비스 종료 후 앱 종료
- Task Scheduler 자동 시작 시 트레이 상태로 시작 (창 숨김)

> **Quit 예외 정책:** 기본은 모든 서비스 종료.
> "서비스 유지하고 앱만 종료"는 고급 옵션(또는 향후 지원)으로 제한.

### Task Scheduler 자동 시작 정책

**원칙:**
- 스케줄러 자동 시작 = DevPort 트레이로 실행 + (옵션) 지정 서비스 자동 시작
- **Quit 시 스케줄러 설정은 유지됨** (다음 부팅에 다시 자동 시작)
- 스케줄러 해제는 "설정 > 자동 시작" 메뉴에서만 가능

> **사용자 안내:** Quit로 앱을 종료해도 "자동 시작" 설정이 켜져 있으면
> 다음 Windows 부팅 시 DevPort가 트레이에 자동으로 실행됩니다.
> 완전히 끄려면 설정에서 "자동 시작"을 해제하세요.

**자동 시작 시 서비스 옵션:**
```
┌─────────────────────────────────────────────────────────────────┐
│  설정 > 자동 시작                                               │
├─────────────────────────────────────────────────────────────────┤
│  ☑ Windows 시작 시 DevPort 자동 실행                           │
│                                                                 │
│  자동 시작할 서비스:                                            │
│  ☑ Apache                                                       │
│  ☑ MariaDB                                                      │
│  ☐ 없음 (트레이만)                                              │
└─────────────────────────────────────────────────────────────────┘
```

### 설치 시 수행되는 작업
1. 바이너리 복사 (`C:\DevPort\runtime\`)
2. Apache/MariaDB 초기 설정
3. MariaDB root 비밀번호 설정 (사용자 입력)
4. 시작 메뉴/바탕화면 바로가기 생성
5. (옵션) Task Scheduler에 자동 시작 등록
6. (옵션) 필요 시 방화벽 예외 등록
   - 대상: `runtime\` 내 주요 바이너리 일괄 등록
     - `httpd.exe`, `mysqld.exe`, `node.exe`, `php.exe`
   - DevPortManager.exe만 등록하면 자식 프로세스 차단될 수 있음

> **hosts 정책:** 설치 시 하지 않음 → **도메인 등록/삭제 시점에 UAC 권한 요청**

> **방화벽 정책 (2단계):**
> - **기본 (127.0.0.1 바인딩):** 방화벽 규칙 생성 안 함
> - **외부 접근 허용 시:** 방화벽 예외 규칙 자동 생성 (`DevPort-Apache-8080` 등)

### 설치 후 빠른 확인 (1분)
1. Apache 시작 → http://localhost:8080 접속 확인
2. phpMyAdmin 열기 → http://localhost:8080/phpmyadmin
3. 새 프로젝트 생성 → 3000 포트 실행 확인

---

### PATH 관리 정책

> **DevPort는 시스템 환경변수를 자동으로 덮어쓰지 않습니다.**
> 런타임 경로(PATH)는 기본적으로 앱 실행 범위에서만 적용됩니다.
> 시스템 PATH 변경은 고급 옵션에서만 제공합니다.

**기본 동작 (권장):**
- DevPort가 프로세스 실행 시 자체 PATH를 주입
- 번들 런타임(Node.js, PHP, Git) 우선 사용
- 시스템 PATH는 변경하지 않음
- **중요:** "DevPort 터미널" 버튼으로 열린 터미널만 번들 PATH 적용
  - 외부 터미널에서는 시스템 Node/PHP 유지

```
[DevPort 내부 실행 시]
PATH = C:\DevPort\runtime\nodejs;C:\DevPort\runtime\php;C:\DevPort\runtime\git\bin;%PATH%
```

**고급 옵션 (선택):**
```
┌─────────────────────────────────────────────────────────────────┐
│  설정 > 고급 > 시스템 PATH                                     │
├─────────────────────────────────────────────────────────────────┤
│  ☐ 시스템 PATH에 DevPort 런타임 추가                           │
│     (외부 터미널에서도 DevPort 런타임 사용)                     │
│                                                                 │
│  ⚠️ 기존 Node.js/PHP 설치와 충돌할 수 있습니다                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 1. 통합 설치 (Bundled Installer)

### 기본 번들 (필수)
- **Git** - 버전 관리 (필수)
- **Apache 2.4** - 웹서버 (포트: 8080)
- **MariaDB 10.x** - 데이터베이스 (포트: 3306)
- **phpMyAdmin** - DB 관리 UI (http://localhost:8080/phpmyadmin)

### PHP 스택
- **PHP 8.x** with Extensions:
  - 기본: `pdo_mysql`, `mbstring`, `openssl`, `curl`, `zip`
  - 프레임워크용: `intl`, `gd`, `fileinfo`, `tokenizer`, `xml`
- **Composer** - PHP 패키지 매니저
- **프레임워크 템플릿**:
  - Laravel (최신)
  - CodeIgniter 4
  - Vanilla PHP

### Node.js 스택
- **Node.js LTS** (20.x)
- **패키지 매니저**:
  - npm (기본)
  - pnpm (권장)
  - yarn
  - corepack 활성화
- **프레임워크 템플릿**:
  - React (Vite)
  - Next.js
  - Vue (Vite)
  - Svelte
  - Vanilla TS

### Python 스택 (번들 제외, 연동 지원)
> **DevPort 번들에 포함되지 않음** - 시스템 Python 감지 및 연동 지원

- **프레임워크 템플릿 지원**:
  - Django
  - Flask
  - FastAPI

#### Python 프로젝트 생성 시
1. DevPort가 시스템에 설치된 Python 자동 감지
2. Python이 없으면 설치 안내 메시지 표시
3. Python 설치 후 프로젝트 생성 가능
4. 포트 관리, 환경변수 관리 등 DevPort 기능 모두 사용 가능

**시스템 Python 감지:**
```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️ Python이 설치되어 있지 않습니다                            │
│                                                                 │
│  Django/Flask/FastAPI 프로젝트를 생성하려면                     │
│  Python을 먼저 설치해주세요.                                    │
│                                                                 │
│  [Python 다운로드 페이지 열기]  [취소]                          │
└─────────────────────────────────────────────────────────────────┘
```

**Python 감지 로직:**
```typescript
// 1. `python --version` 실행
// 2. 출처 확인:
//    - Store stub: "Python was not found..." 메시지
//    - 실제 Python: 버전 번호 출력
// 3. PATH에서 python.exe 경로 확인
//    - Store stub: %LOCALAPPDATA%\Microsoft\WindowsApps\
//    - 실제: C:\Python3xx\ 등
// 4. 실제 경로 우선 사용 (where python)
```
- 감지된 경로 표시: `C:\Python312\python.exe`

---

## 2. 서비스 컨트롤 패널 (A)

```
┌──────────────────────────────────────────────────────────────────────┐
│  Service        Port        Status      Actions                      │
├──────────────────────────────────────────────────────────────────────┤
│  🟢 Apache      8080   Running     [Stop] [Restart] [Logs] [⚙️]│
│  🟢 MariaDB     3306        Running     [Stop] [Restart] [Logs] [⚙️]│
├──────────────────────────────────────────────────────────────────────┤
│  Tools                                                               │
│  📋 phpMyAdmin  http://localhost:8080/phpmyadmin    [Open in Browser]│
└──────────────────────────────────────────────────────────────────────┘
```

> **참고:** Redis 등 추가 서비스는 MVP에 포함되지 않음.
> 향후 플러그인 방식으로 제공 예정.

> **phpMyAdmin 분류:**
> - phpMyAdmin은 독립 프로세스가 아니라 **Apache 경로(`/phpmyadmin`)로 제공되는 도구**
> - Service가 아닌 Tool로 분류 (`dependsOn: apache`)
> - 상태는 "Apache 실행 중 + `/phpmyadmin` 엔드포인트 체크"로 판단

### 기본 포트 설정
| 서비스 | 포트 | 비고 |
|--------|------|------|
| Apache | 8080 | HTTP only (MVP) |
| MariaDB | 3306 | - |

> **참고:** 80/443 포트는 다른 프로그램과 충돌 가능성이 높아 8080 사용
> **SSL:** MVP 미지원, Phase 2 이후 추가 예정

### 기능 상세
- **시작/중지/재시작** - 각 서비스 개별 제어
- **자동 재시작** - 크래시 시 자동 복구 (옵션)
- **로그 뷰어** - stdout/stderr 분리 표시
- **설정 편집**:
  - 실행 명령어 수정
  - 환경변수(ENV) 편집
  - 포트 변경
  - 설정 파일 열기 (httpd.conf, my.ini 등)

> **Stop 정책:**
> - **Services Stop:** 해당 서비스 프로세스 종료 (Apache/MariaDB)
> - **Projects Stop 순서:**
```typescript
stopPolicy: {
  stopCommand: {
    timeout: 10000,           // 10초
    onTimeout: 'skip'         // 타임아웃 시 다음 단계로
  },
  processTree: {
    timeout: 5000,            // 5초
    onTimeout: 'force-kill'   // 타임아웃 시 강제 종료
  },
  portCleanup: {
    enabled: false,           // 기본 비활성화 (fallback)
    warnOnly: true            // 경고만 표시
  }
}
// 최대 총 시간: 15초 (stopCommand 10초 + processTree 5초)
```
> - **Windows 프로세스 종료 (핵심 원칙):**
>   - **Job Object 사용 (1차 방어선):** DevPort가 자식 프로세스 생성 시 Job Object에 등록
>     - DevPort 종료 시 Windows 커널이 하위 프로세스 자동 정리
>     - DevPort가 시작한 프로세스만 종료됨 (다른 Node/Python 프로세스 보호)
>   - **Node.js:** Job Object 기반 종료 (권장)
>   - **Python:** CTRL+C 시뮬레이션 → fallback으로 taskkill /T
>   - **기타:** taskkill /T /F 사용
> - **강제 포트 해제 (fallback):**
>   - Job Object/stopCommand 실패 시 해당 프로젝트 포트를 점유 중인 PID만 Kill
>   - **주의:** 해당 프로젝트 포트에 한정 (무관한 프로세스 보호)
>   - Phase 1 코어 기능에 포함

### 상태 모델 (Health Check)
단순 running/stopped가 아닌 4단계 상태 관리:

| 상태 | 아이콘 | 의미 | 판단 기준 |
|------|--------|------|-----------|
| Running | 🟢 | 정상 실행 | PID 존재 + Health Check 통과 |
| Stopped | ⚫ | 중지됨 | PID 없음 |
| Error | 🔴 | 시작 실패/크래시 | 프로세스 비정상 종료 |
| Unhealthy | 🟡 | 응답 없음 | PID 존재 + Health Check 실패 |

**Health Check 엔드포인트:**
- **Apache:** `GET http://localhost:8080/` (200 응답)
- **phpMyAdmin:** Apache 실행 중 + `GET http://localhost:8080/phpmyadmin/` (200/302 허용)
- **MariaDB:** TCP connect + (옵션) `mysqladmin ping`
  - TCP 연결 성공으로 Running 판정 (인증 불필요)
- **프로젝트 (Node/Python):** PID 존재 + 포트 리스닝 확인
  - 기본: TCP 리스닝 확인
  - 선택: HTTP 엔드포인트(`/health`)를 프로젝트별로 추가 설정 가능

**상태 전환 규칙 (기본값):**
```typescript
healthCheck: {
  interval: 5000,      // 5초마다 체크 (부하 감소)
  timeout: 2000,       // 2초 대기
  retries: 2,          // 2회 실패 시 Unhealthy (총 10초)
}
```
- **Unhealthy 진입:** retries 초과 시
- **Error 진입:** 프로세스 종료/시작 실패 시
- **AutoRestart 동작:** Unhealthy 지속 시 재시작 시도 (최대 5회)
- **재시작 제한 초과 시:** Error 고정 + 수동 개입 유도

### 로그 관리 정책

```typescript
interface LogConfig {
  maxFileSize: '10MB' | '50MB' | '100MB';   // 단일 파일 최대 크기
  maxFiles: number;                          // 보관할 파일 수 (기본 5)
  rotationPolicy: 'size' | 'daily';          // 로테이션 기준
  retentionDays: number;                     // 보관 일수 (기본 30)
}
// daily 선택 시: 매일 자정(00:00)에 로테이션
// 파일명 예: apache_2024-01-26.log
```

**기본 설정:**
- 파일당 최대 50MB
- 최대 5개 파일 유지 (로테이션)
- 30일 이상 된 로그 자동 삭제
- 서비스별 개별 로그 파일:
  ```
  C:\DevPort\logs\
  ├── apache\
  │   ├── access.log
  │   ├── error.log
  │   └── error.log.1 (로테이션)
  ├── mariadb\
  │   └── mysql.log (general/error 등 설정에 따라)
  └── projects\
      └── my-app\
          ├── stdout.log
          └── stderr.log
  ```

---

## 3. 프로젝트 관리 (B)

### 새 프로젝트 생성
```
┌─────────────────────────────────────────────────────────────────┐
│  + New Project                                                  │
├─────────────────────────────────────────────────────────────────┤
│  📁 Location: [C:\DevPort\projects\my-app    ] [Browse] [New]   │
│                                                                 │
│  Framework:                                                     │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │ React/Vite  │ Next.js     │ Vue/Vite    │ Svelte      │     │
│  ├─────────────┼─────────────┼─────────────┼─────────────┤     │
│  │ Laravel     │ CodeIgniter │ Django      │ FastAPI     │     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘     │
│                                                                 │
│  Port: [3000]  ✅ Available                                     │
│  Domain (optional): [my-app.test]                              │
│                                                                 │
│  Package Manager: ○ npm  ● pnpm  ○ yarn                        │
│                                                                 │
│  [Create Project]                                               │
└─────────────────────────────────────────────────────────────────┘
```

### 프로젝트 카드 (등록된 프로젝트)
```
┌─────────────────────────────────────────────────────────────────┐
│  📦 my-nextjs-app                              [Next.js]        │
├─────────────────────────────────────────────────────────────────┤
│  📁 C:\DevPort\projects\my-nextjs-app                          │
│  🌐 Port: 3000  |  Domain: my-app.test                        │
│  📊 Status: 🟢 Running (PID: 12345)                            │
│                                                                 │
│  [Stop] [Restart] [Logs] [Terminal] [VS Code] [Browser]        │
│  [Edit .env] [DB Dump] [DB Restore] [Settings]                 │
└─────────────────────────────────────────────────────────────────┘
```

### 프로젝트 레지스트리 (저장 정보)
```typescript
interface Project {
  id: string;
  name: string;
  path: string;
  framework: 'nextjs' | 'vite-react' | 'vue' | 'svelte' |
             'laravel' | 'codeigniter' | 'django' | 'flask' | 'fastapi' |
             'vanilla-ts' | 'vanilla-php';

  // 포트/도메인
  port: number;
  domain?: string;              // hosts 파일 연동

  // 실행 설정
  startCommand: string;
  stopCommand?: string | {      // 커스텀 중지 명령 (옵션)
    command: string;
    args?: string[];
    timeout?: number;
  };
  buildCommand?: string;        // 빌드 명령 (옵션)
  installCommand?: string;      // 의존성 설치 명령 (옵션)
  packageManager?: 'npm' | 'pnpm' | 'yarn';

  // 환경 (로컬 저장 프로필 - 운영 서버 배포용이 아님)
  envProfile: 'development' | 'staging' | 'production';
  envFile?: string;             // 커스텀 env 파일 경로

  // 자동화
  autoStart: boolean;
  autoBackupDb: boolean;        // 시작 전 DB 백업 (NEW)

  // 상태 (런타임)
  status: 'running' | 'stopped' | 'error' | 'unhealthy';  // 4단계 상태
  pid?: number;
  lastStarted?: string;         // ISO 날짜
  lastStopped?: string;

  // 데이터베이스 (NEW: 확장)
  database?: {
    name: string;
    user: string;
    password: string;           // 암호화 저장
    host: string;               // 기본 localhost
    port: number;               // 기본 3306
  };

  // 메타데이터 (NEW)
  createdAt: string;
  updatedAt: string;
  notes?: string;               // 사용자 메모
  tags?: string[];              // 프로젝트 분류 태그
}
```

### Service 모델
```typescript
interface Service {
  id: string;
  name: string;                 // Apache, MariaDB, etc.
  type: 'webserver' | 'database' | 'runtime' | 'tool';

  // 실행 설정
  executable: string;           // 실행 파일 경로
  args: string[];               // 실행 인자
  workDir: string;              // 작업 디렉토리
  env: Record<string, string>;  // 환경 변수

  // 포트
  port: number;
  additionalPorts?: number[];   // 예: Apache 80, 443

  // 상태
  status: 'running' | 'stopped' | 'error' | 'unhealthy';
  pid?: number;

  // 자동화
  autoStart: boolean;
  autoRestart: boolean;
  restartDelay: number;         // ms
  maxRestarts: number;          // 무한 재시작 방지

  // Health Check
  healthCheck: {
    type: 'http' | 'tcp' | 'process';
    endpoint?: string;          // HTTP의 경우 URL
    interval: number;           // ms
    timeout: number;            // ms
    retries: number;
  };

  // 로그
  logConfig: {
    stdout: string;             // stdout 로그 경로
    stderr: string;             // stderr 로그 경로
    maxSize: string;            // 예: '50MB'
    maxFiles: number;
  };
}
```

### 원클릭 열기
- **브라우저** - http://localhost:3000 또는 http://my-app.test:3000 (포트 포함)
- **VS Code** - `code <project-path>`
- **터미널** - 해당 폴더에서 PowerShell/CMD 열기
- **phpMyAdmin** - http://localhost:8080/phpmyadmin (프로젝트 DB 선택 상태로)

---

## 4. 데이터베이스 관리

### phpMyAdmin 통합
- DevPort 내장 (별도 설치 불필요)
- **Apache 서브디렉토리로 접근**: `http://localhost:8080/phpmyadmin`
- Apache의 Alias 설정으로 연동

**Apache 설정 (httpd.conf):**
```apache
# phpMyAdmin 설정
Alias /phpmyadmin "C:/DevPort/tools/phpmyadmin"
<Directory "C:/DevPort/tools/phpmyadmin">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require local
</Directory>
```

> **권한:** phpMyAdmin 디렉터리는 읽기 권한(Users) 부여.
> Apache 실행 사용자 권한으로 접근 가능해야 함.

> **Apache 실행 권한:**
> Apache는 Windows 서비스가 아니며, **DevPort 앱 사용자 권한**으로 실행됩니다.

### 보안 정책

#### MariaDB 보안
| 항목 | 설정 |
|------|------|
| 바인딩 | `127.0.0.1` (기본, localhost only) |
| root 계정 | 설치 시 사용자가 비밀번호 설정 |
| 사용자 DB/계정 | phpMyAdmin에서 직접 생성 |
| 외부 접근 | 기본 OFF, 고급 메뉴에서만 활성화 |

> **MariaDB 외부 접근 정책:**
> - **기본:** `bind-address = 127.0.0.1` (OFF)
> - **고급 활성화 조건:** IP allowlist **필수** 입력
> - **활성화 시:** 자동으로 `bind-address = 0.0.0.0` 변경
> - **DB 계정:** `user@'%'` 금지 → `user@'<specific-ip>'` 형태로만 생성

#### phpMyAdmin 보안
| 항목 | 설정 |
|------|------|
| 접근 경로 | `http://localhost:8080/phpmyadmin` |
| 접근 제한 | `Require local` (기본, localhost만) |
| 인증 | cookie 방식 (아래 설명 참조) |
| 블로우피시 비밀 | 설치 시 자동 생성 (32자 랜덤) |

**phpMyAdmin 접근 제한 정책:**
```apache
# 기본 (localhost만)
<Directory "C:/DevPort/tools/phpmyadmin">
    Require local
</Directory>

# 외부 접근 허용 시 (IP 제한 필수)
<Directory "C:/DevPort/tools/phpmyadmin">
    Require ip 127.0.0.1 <allowlist-ip>
</Directory>
```

**cookie 인증 방식 설명:**
- phpMyAdmin 로그인 화면에서 DB 계정(사용자명/비밀번호)으로 로그인
- 비밀번호 없는 계정 로그인 차단 (`AllowNoPassword = false`)
- 세션 쿠키로 로그인 상태 유지
- 사용자는 프로젝트 전용 계정으로 로그인 권장 (root 사용 지양)

> **외부 접근 정책:** 외부 접근 허용은 기본 미지원 (또는 고급 메뉴에서만).
> 활성화 시 보안 경고 + 가이드(강한 비밀번호, IP 제한 등) 표시.

> **⚠️ 보안 경고:** phpMyAdmin을 외부에 노출하면 DB 전체가 노출됩니다.
> 반드시 강력한 비밀번호 + IP 제한을 함께 사용하세요.

**phpMyAdmin config.inc.php:**
```php
<?php
$cfg['blowfish_secret'] = 'auto_generated_32char_secret';
$cfg['Servers'][1]['host'] = 'localhost';
$cfg['Servers'][1]['auth_type'] = 'cookie';
$cfg['Servers'][1]['AllowNoPassword'] = false;  // 비밀번호 필수
?>
```

**DB 계정 정책 (MVP: 자동 생성 기본):**
- **프로젝트 생성 시:** 전용 DB + 계정 자동 생성 (핵심 차별점)
- **phpMyAdmin:** 고급 관리 도구로 제공 (수동 관리용)
- **root 계정:** 초기 설치 시 설정, DevPort 내부 자동화에 사용
- 비밀번호 없는 계정 로그인 차단 (`AllowNoPassword = false`)

> **MVP 방침:** 프로젝트 생성 시 자동 DB/계정 생성이 기본입니다.
> phpMyAdmin은 사용자 고급 관리 도구로 제공됩니다.

**root 비밀번호 관리:**
- 설치 시 root 비밀번호 설정 (사용자 입력)
- DPAPI로 로컬 암호화 저장 (자동 로그인 등에 사용)
- 설정 > 고급에 "암호화 키 재설정" 가이드 제공
  - Windows 비밀번호 변경/계정 이동 시 대비

**DPAPI 복호화 실패 시 복구 절차:**
1. `%APPDATA%/devport-manager/credentials.enc` 삭제
2. DevPort 재시작 → root 비밀번호 재입력 안내
3. (앱 내부에 복구 가이드 내장)

**MariaDB 초기화 실패 처리:**
```
┌─────────────────────────────────────────────────────────────────┐
│  MariaDB 초기화 실패                                            │
│                                                                 │
│  원인: 기존 MySQL 서비스가 3306 사용 중                         │
│                                                                 │
│  [3307로 변경]  [기존 MySQL 유지하고 설치 중단]                 │
└─────────────────────────────────────────────────────────────────┘
```

**실패 케이스 분기:**
1. 포트 충돌 → 포트 변경 제안
2. data 폴더 권한 문제 → 권한 수정 가이드
3. 기존 MySQL 설치 충돌 → 공존 또는 중단 선택

### 프로젝트별 DB 계정 자동화

프로젝트 생성 시 자동으로 전용 DB와 계정 생성:

```typescript
// 프로젝트 생성 시 자동 실행
interface DatabaseSetup {
  database: string;        // 예: my_app_db
  user: string;            // 예: my_app_user
  password: string;        // 자동 생성 (16자 랜덤)
  privileges: 'ALL';       // 해당 DB에만 권한
}
```

**자동 생성 SQL:**
```sql
CREATE DATABASE IF NOT EXISTS `my_app_db`;
CREATE USER 'my_app_user'@'localhost' IDENTIFIED BY 'auto_generated_pw';
GRANT ALL PRIVILEGES ON `my_app_db`.* TO 'my_app_user'@'localhost';
FLUSH PRIVILEGES;
```

**.env 자동 설정:**
```env
# 프로젝트 생성 시 .env에 자동 추가
DATABASE_URL=mysql://my_app_user:auto_generated_pw@localhost:3306/my_app_db
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=my_app_db
DB_USERNAME=my_app_user
DB_PASSWORD=auto_generated_pw
```

### 프로젝트별 DB 관리 UI
```
┌─────────────────────────────────────────────────────────────────┐
│  Database: my_app_db                                            │
│  User: my_app_user (dedicated)                                  │
├─────────────────────────────────────────────────────────────────┤
│  [📤 Dump]     SQL 파일로 내보내기                              │
│  [📥 Restore]  SQL 파일에서 복원                                │
│  [🔄 Reset]    DB 초기화 (주의!)                                │
│  [🔑 Reset Password] 비밀번호 재생성                            │
│  [📋 Open in phpMyAdmin]                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 백업 저장 위치
```
C:\DevPort\backups\
├── my-app\
│   ├── YYYY-MM-DD_HHmmss_my_app_db.sql  (자동 백업)
│   ├── YYYY-MM-DD_manual_my_app_db.sql  (수동 덤프)
│   └── latest.txt (최신 백업 파일명 기록)
└── another-project\
    └── ...
```
> **파일명 규칙:** 타임스탬프 포함으로 하루 여러 번 백업 지원

> **Windows 호환:** `latest.sql` 심볼릭 링크 대신 `latest.txt` 포인터 파일 사용.
> (Windows 심볼릭 링크는 관리자 권한/개발자 모드 필요)

### 백업 정책
- 수동 덤프 + 선택적 자동 백업
- 자동 백업: 프로젝트 시작 전 (옵션)
- 보관 기간: 최근 10개 또는 30일
- **자동 백업 실패 시:** 시작은 계속 진행 (경고 표시만)
- 타임아웃: 일정 시간 초과 시 백그라운드로 전환

### DB 덤프/복원 옵션
**Dump 기본 옵션:**
- `--routines --triggers --single-transaction` (InnoDB)
- 인코딩 기본값: `utf8mb4`

**Restore 처리:**
- **대용량 (수백 MB 이상):**
  - mysql.exe에 파일 파이핑 시 전체 파일 크기 대비 현재 읽은 바이트 수 계산
  - 프로그레스 바 표시 (예: "복원 중... 45% (123MB / 274MB)")
  - UI 멈춤 방지: 별도 스레드에서 실행
- 실패 시: 오류 라인 표시 + "재시도" 버튼

---

## 5. 환경변수 매니저 (C)

### .env GUI 편집기
```
┌─────────────────────────────────────────────────────────────────┐
│  Environment Variables - my-nextjs-app                         │
│  Profile: [Development ▼]                                       │
├─────────────────────────────────────────────────────────────────┤
│  DATABASE_URL    │ mysql://my_app_user:****@localhost:3306/my_app│
│  API_KEY         │ ******************************** [👁️]       │
│  NODE_ENV        │ development                                 │
│  PORT            │ 3000                                        │
│  + Add Variable                                                │
├─────────────────────────────────────────────────────────────────┤
│  Profiles: [Development] [Staging] [Production] [+ New]        │
│                                                                 │
│  [Save] [Copy from Template] [Export] [Import]                 │
└─────────────────────────────────────────────────────────────────┘
```

> **참고:** DATABASE_URL은 프로젝트 전용 계정 사용 (root 계정 아님)

### 템플릿 제공
- **Next.js 기본** - DATABASE_URL, NEXTAUTH_SECRET 등
- **Laravel 기본** - APP_KEY, DB_CONNECTION 등
- **Django 기본** - SECRET_KEY, DEBUG, DATABASE_URL 등

### 프로필 기능
- **Development** - 로컬 개발용
- **Staging** - 테스트 서버용
- **Production** - 운영용 (마스킹 처리)

---

## 6. 포트/도메인 매핑

### 포트 대시보드 (개발 관련만)
```
┌──────────────────────────────────────────────────────────────────┐
│  Port     Service/Project      Domain           Status          │
├──────────────────────────────────────────────────────────────────┤
│  8080     Apache               localhost        🟢 Running      │
│  8443     Apache (SSL)         localhost        🟢 Running      │
│  3000     my-nextjs-app        my-app.test     🟢 Running      │
│  3001     my-react-app         react.test      ⚫ Stopped      │
│  3306     MariaDB              -                🟢 Running      │
│  5173     my-vite-app          vite.test       🟡 Conflict!    │
└──────────────────────────────────────────────────────────────────┘
```

> **참고:** phpMyAdmin은 Apache의 서브디렉토리(`/phpmyadmin`)로 접근하므로 별도 포트 없음

### hosts 파일 자동 관리
- 도메인 등록 시 자동으로 hosts 파일에 추가
- `127.0.0.1 my-app.test` 자동 추가/제거
- 도메인 등록/삭제 시점에 UAC 권한 요청

> **권장 TLD:** `.test` (RFC 6761 예약)
> `.local`은 mDNS/Bonjour와 충돌 가능 → 고급 설정에서만 허용

> **hosts 편집 안정성:**
> - DevPort 전용 marker 구간 사용 (`# DevPort BEGIN` ~ `# DevPort END`)
> - 멱등(idempotent)하게 적용 - 수동 편집이 있어도 중복/누락 방지

> **도메인 충돌 처리:**
```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️ 도메인 충돌                                                │
│                                                                 │
│  my-app.test는 이미 다른 프로그램이 등록했습니다.              │
│                                                                 │
│  [새 도메인 추천]  [취소]                                      │
│  (* "덮어쓰기"는 기본 제공 안 함 - 안전 우선)                  │
└─────────────────────────────────────────────────────────────────┘
```

> **도메인-서버 연결 정책:**
> - **기본 (hosts만):** `http://my-app.test:3000` (포트 포함)
> - **MVP 옵션 (Apache Reverse Proxy):**
>   - `http://my-app.test` (포트 없이) 접속 지원
>   - Apache VirtualHost로 80 → 프로젝트 포트 프록시
>   - 지원 프레임워크: Next.js, Vite (React/Vue), Svelte
>   - 프로젝트 설정에서 "도메인 전용 접속" 체크박스로 활성화

**Apache VirtualHost 예시 (자동 생성):**
```apache
<VirtualHost *:80>
    ServerName my-app.test
    ProxyPreserveHost On
    ProxyPass / http://localhost:3000/
    ProxyPassReverse / http://localhost:3000/
</VirtualHost>
```

> **제약사항:**
> - Apache가 실행 중이어야 함
> - 80 포트 사용 (다른 프로그램과 충돌 가능 → 충돌 시 안내)
> - SSL(443)은 Phase 2에서 지원

**도메인 접속 예시:**
- **기본:** `http://my-app.test:3000` (포트 포함)
- **Reverse Proxy 활성화 시:** `http://my-app.test` (포트 없이)

---

## 설치 구조

```
C:\DevPort\
├── DevPortManager.exe           <- 메인 앱
├── runtime\
│   ├── apache\
│   │   ├── bin\
│   │   ├── conf\httpd.conf
│   │   └── htdocs\
│   ├── mariadb\
│   │   ├── bin\
│   │   └── data\
│   ├── php\
│   │   ├── php.exe
│   │   ├── php.ini
│   │   └── ext\                 <- PHP 확장
│   ├── nodejs\
│   │   ├── node.exe
│   │   ├── npm
│   │   └── pnpm
│   └── git\
│       └── bin\git.exe
├── tools\
│   ├── phpmyadmin\              <- Apache /phpmyadmin으로 접근
│   └── composer\composer.phar
├── projects\                    <- 기본 프로젝트 위치
├── backups\                     <- DB 백업
├── config\
│   └── devport.json
└── logs\
```

> **참고:** Python은 번들에 포함되지 않음. 시스템에 설치된 Python 사용.

---

## MVP 구현 순서

### Phase 1: 코어 인프라 (Foundation)
> 서비스/프로젝트 관리의 기반이 되는 핵심 기능

- [ ] **Service 모델 구현**
  - Service 타입 정의 (Rust + TypeScript)
  - 4단계 상태 모델 (running/stopped/error/unhealthy)
  - Health Check 시스템

- [ ] **로그 시스템**
  - 로그 파일 관리 (stdout/stderr 분리)
  - 로그 로테이션 정책
  - 실시간 로그 스트리밍 (Tauri events)

- [ ] **프로세스 관리**
  - 프로세스 시작/정지/재시작
  - 자동 재시작 로직
  - 크래시 감지 및 복구

### Phase 2: 서비스 관리 (Services Tab)
> 핵심 서비스들의 실행 및 관리

- [ ] **Apache 통합**
  - httpd 시작/정지
  - httpd.conf 수정 UI
  - 가상 호스트 관리 (MVP: /phpmyadmin, 로컬 랜딩 페이지 등 내부 도구 노출용. 프로젝트 프록시는 향후)

- [ ] **MariaDB 통합**
  - mysqld 시작/정지
  - my.ini 설정
  - root 비밀번호 설정 (사용자 입력)

- [ ] **phpMyAdmin (Apache 연동)**
  - Apache Alias로 `/phpmyadmin` 설정
  - `http://localhost:8080/phpmyadmin` 접근
  - cookie 인증 방식

- [ ] **Services 탭 UI**
  - 서비스 목록 (상태 표시)
  - 시작/정지/재시작 버튼
  - 로그 뷰어 (실시간)

### Phase 3: 프로젝트 관리 (Projects Tab)
> 프로젝트 생성, 실행, 데이터베이스 연동

- [ ] **프로젝트 생성 마법사**
  - 프레임워크 선택 UI
  - 폴더 선택/생성
  - 포트 자동 할당

- [ ] **DB 자동화**
  - 프로젝트별 DB/계정 자동 생성
  - .env 자동 설정
  - 비밀번호 암호화 저장

- [ ] **DB 덤프/복원**
  - mysqldump 연동
  - 복원 기능
  - 백업 이력 관리

- [ ] **원클릭 열기**
  - 브라우저 (localhost:port)
  - VS Code (`code <path>`)
  - 터미널 (PowerShell)

### Phase 4: 환경변수 & 도메인
> .env 관리 및 로컬 도메인 설정

- [ ] **.env 편집기**
  - GUI 편집 UI
  - 민감 값 마스킹
  - 템플릿에서 생성

- [ ] **프로필 관리**
  - development/staging/production 프로필
  - 프로필 전환
  - 프로필 복사

- [ ] **hosts 파일 관리**
  - 도메인 → localhost 매핑
  - 관리자 권한 요청
  - 자동 추가/제거

### Phase 5: 번들 & 인스톨러
> 바이너리 패키징 및 배포

- [ ] **바이너리 번들링**
  - Apache, MariaDB, PHP, Node.js, Git (Python 제외)
  - 상대 경로 설정
  - 최초 실행 시 초기화
  - MariaDB root 비밀번호 설정 프롬프트

- [ ] **NSIS 인스톨러**
  - 설치 옵션 UI
  - 설치 경로 선택
  - 시작 메뉴/바탕화면 아이콘
  - Task Scheduler 자동 시작 등록 (옵션)

- [ ] **업데이트 시스템** (Optional)
  - GitHub Releases 확인
  - 인앱 업데이트

- [ ] **언인스톨러**
  - 3단계 삭제 모드 (기본/전체/시스템원복)
  - 시스템 변경 마킹 추적 제거
  - 프로세스 종료 + 포트 해제 확인
  - 삭제 실패 시 재부팅 예약 옵션

---

## 우선순위 노트

### 현재 구현된 것 (기존 앱)
- ✅ Tauri 2 + React 19 + Tailwind 3 셋업
- ✅ 포트 스캔 기능
- ✅ 프로젝트 등록/삭제
- ✅ 프로젝트 시작/정지 (기본)
- ✅ 기본 UI 레이아웃

### 다음 단계
1. **Phase 1** - 코어 인프라 강화 (Service 모델, 로그, Health Check)
2. **Phase 2** - Apache + MariaDB 통합 (실제 서비스 관리)
3. **Phase 3** - DB 자동화가 핵심 (사용자 경험 차별화 포인트)

### 후순위 (Nice to have)
- 다중 런타임 버전 관리 (Node.js, PHP)
- Redis 등 추가 서비스 (아래 참조)
- 원격 DB 연결
- 협업 기능
- Windows 서비스 등록 (고급 옵션)

### DevPort Config Schema
```json
{
  "schemaVersion": 1,
  "services": [],
  "projects": []
}
```
- 향후 마이그레이션 대비 버전 관리

### Crash Recovery 정책
DevPort 실행 중 시스템 재부팅/강제 종료 시:
- 다음 실행에서 **PID stale cleanup** 수행
- **포트 점유 상태 재스캔**
- `autoStart` 프로젝트만 복원

### 개인정보 보호 정책
> **DevPort는 어떠한 사용자 코드/DB/로그도 외부로 전송하지 않습니다.**
> Telemetry/Analytics 없음.

### 차별화 포인트 (제품 포지션 강화)
- **프로젝트별 런타임 스냅샷:** node -v, php -v, composer -V, pnpm -v 표시 (읽기 전용)
- **실행 레시피 표준화:** startCommand/buildCommand + `installCommand` 필드 추가 (옵션)
- **백업/복원 사전 점검:** Restore 전 대상 DB 존재/권한 확인 → 없으면 자동 생성 (옵션)
- **로그 에러 하이라이트:** stderr에서 패턴 탐지(포트 점유/권한/모듈 누락) → 요약 카드 표시
- **포트/도메인 충돌 매트릭스:** 포트 + 도메인 중복/hosts 충돌 통합 관리
- **DevPort JSON 스키마 버전:** `config/devport.json`에 `"schemaVersion": 1` 추가 (마이그레이션 대비)

### Redis 지원 정책

> **Redis는 고급 개발 기능(캐시/세션/큐)을 위한 선택 서비스입니다.**
> 기본 설치에는 포함되지 않으며,
> 필요한 사용자만 추가로 활성화할 수 있습니다.

**MVP에서는 미포함:**
- 대부분의 초보 사용자에게 불필요
- 설치 용량 및 복잡성 증가

**향후 확장 (플러그인 방식):**
- "서비스 추가" 메뉴에서 Redis 다운로드/활성화
- Laravel, Django 등 프레임워크에서 캐시 드라이버로 사용

---

## 기술 스택

### 앱
- **Tauri 2** (Rust + WebView)
- **React 19 + TypeScript**
- **Tailwind CSS 3**
- **Zustand** (상태관리)

### 인스톨러
- **NSIS** (Windows 인스톨러)

### 번들 바이너리
| 소프트웨어 | 소스 | 크기 | 비고 |
|-----------|------|------|------|
| Apache 2.4 | apachelounge.com | ~15MB | 필수 |
| MariaDB 10 | mariadb.org | ~200MB | 필수 |
| PHP 8.x | windows.php.net | ~30MB | 필수 |
| Node.js 20 | nodejs.org | ~70MB | 필수 |
| Git | git-scm.com | ~50MB | 필수 |
| phpMyAdmin | phpmyadmin.net | ~15MB | 필수 |
| **Total** | | **~380MB** | |

### 번들에 포함되지 않음
| 소프트웨어 | 이유 | 대안 |
|-----------|------|------|
| Python | 사용자 환경마다 버전/패키지 요구사항 다름 | 시스템 Python 감지 및 안내 |

### 라이선스
> 각 번들 구성요소는 원저작 라이선스를 준수하며,
> 설치 폴더에 라이선스 정보를 포함합니다.
> - 경로: `C:\DevPort\LICENSES\` 또는 `C:\DevPort\NOTICE.txt`

---

## 9. 언인스톨 정책 (Clean Uninstall)

DevPort는 번들형 통합 개발환경이므로, **설치·생성·설정한 모든 항목을 완전 삭제할 수 있는 기능**을 제공합니다.

> **핵심 원칙:**
> 1. DevPort가 설치하는 번들(Apache/MariaDB/PHP/Node/Git/phpMyAdmin)은 기본적으로 `C:\DevPort\` 하위에만 배치되며, **폴더 삭제로 완전 제거**가 가능하도록 설계한다.
> 2. DevPort가 시스템에 추가하는 항목(Task Scheduler/hosts/방화벽/바로가기/(선택) PATH)은 모두 `DevPort-*` 규칙으로 마킹/추적하고, **Uninstall C에서 원복(완전 제거)**한다.
> 3. DevPort 설정/캐시가 `%LOCALAPPDATA%\DevPort`에 저장되는 경우, **Uninstall 모드별(A/B/C)로 삭제 범위를 명시**하고 선택 삭제를 제공한다.

### 3단계 Uninstall 모드

#### A. 기본 삭제 (권장)
DevPort 앱과 번들 런타임만 삭제:

**삭제 대상:**
- `C:\DevPort\DevPortManager.exe`
- `C:\DevPort\runtime\` (Apache/MariaDB/PHP/Node/Git)
- `C:\DevPort\tools\` (phpMyAdmin, composer)
- `C:\DevPort\config\`

**유지:**
- `C:\DevPort\projects\` (사용자 프로젝트)
- `C:\DevPort\backups\` (DB 백업)
- `C:\DevPort\logs\`
- `%LOCALAPPDATA%\DevPort` (설정/캐시)

#### B. 전체 데이터 포함 삭제 (옵션)
A + 사용자 생성 데이터까지 완전 삭제:

**추가 삭제:**
- `C:\DevPort\projects\`
- `C:\DevPort\backups\`
- `C:\DevPort\logs\`
- `runtime\mariadb\data` (DB 데이터)

⚠️ "데이터 삭제" 경고 + 체크박스 2중 확인 필수

#### C. 시스템 변경까지 완전 원복 (고급)
B + DevPort가 시스템에 추가한 설정까지 제거:

**추가 원복:**
- Task Scheduler 자동 시작 작업 (`DevPort-AutoStart`)
- Windows 방화벽 예외 규칙 (`DevPort-*`)
- hosts 파일 도메인 매핑 (`# DevPort BEGIN` ~ `# DevPort END`)
- 시작 메뉴/바탕화면 바로가기
- `%LOCALAPPDATA%\DevPort` (설정/캐시)

### 안전한 삭제를 위한 설계 원칙

1. **설치 경로 단일화** - 모든 번들은 `C:\DevPort\` 아래에만 존재
2. **시스템 변경 마킹** - hosts/방화벽/스케줄러 모두 `DevPort-*` 접두사로 추적
3. **MariaDB 데이터 경로 고정** - `runtime\mariadb\data` (삭제 경계 명확화)
4. **삭제 전 종료 절차** - Apache/MariaDB/프로젝트 프로세스 종료 + 포트 해제 확인
5. **삭제 실패 대비** - 파일 잠김 시 "재부팅 후 완전 삭제" 예약 옵션

### Uninstall UI
```
┌─────────────────────────────────────────────────────────────────┐
│  DevPort 제거                                                   │
├─────────────────────────────────────────────────────────────────┤
│  ☑ DevPort 앱 + 번들 런타임 삭제 (필수)                        │
│  ☐ 프로젝트/DB/백업까지 삭제 (데이터 삭제)                     │
│  ☐ hosts/방화벽/자동시작까지 원복 (관리자 권한 필요)           │
│                                                                 │
│  ⚠️ "데이터 삭제" 선택 시 다음 경로가 삭제됩니다:              │
│     C:\DevPort\projects\                                        │
│     C:\DevPort\runtime\mariadb\data                             │
│                                                                 │
│  [제거]  [취소]                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 버전 관리 (Runtime Version Management)

### MVP에서는 단일 버전
초기 버전에서는 각 런타임의 최신 LTS 버전만 번들:
- Node.js 20.x LTS
- PHP 8.3.x
- ~~Python~~ (번들 제외 - 시스템 Python 사용)

> **향후 고려사항 (Phase 2+):**
> - 현재: mod_php 모듈 방식
> - 향후: FastCGI(mod_fcgid) 전환 검토
>   - 다중 PHP 버전 지원 시 유리
>   - MVP 단계에서 구조 변경 최소화를 위해 Phase 2 이후 검토

### 향후 확장: 다중 버전 지원
```
┌─────────────────────────────────────────────────────────────────┐
│  Runtime Versions                                               │
├─────────────────────────────────────────────────────────────────┤
│  Node.js                                                        │
│    ● 20.11.0 (LTS) [Default] [Active]                          │
│    ○ 18.19.0 (LTS) [Download]                                  │
│    ○ 21.6.0       [Download]                                   │
│                                                                 │
│  PHP                                                            │
│    ● 8.3.2        [Default] [Active]                           │
│    ○ 8.2.15       [Download]                                   │
│    ○ 7.4.33       [Download]                                   │
│                                                                 │
│  [Check for Updates]                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 프로젝트별 버전 선택
```typescript
interface Project {
  // ... 기존 필드
  runtimeVersions?: {
    node?: string;    // 예: '20.11.0'
    php?: string;     // 예: '8.3.2'
    // Python은 시스템 버전 사용
  };
}
```

### 시스템 런타임 감지
- DevPort 시작 시 시스템에 설치된 런타임 자동 감지
- **Python**: 시스템 Python만 감지 (번들 없음)
- **Node.js/PHP**: 번들 버전 기본 사용
- **MVP 범위:** 시스템 버전 "감지/표시"만 지원, "선택" 기능은 향후

---

## 8. 사용자 경험 (UX) 개선

### 오류 복구 가이드 (원클릭)
일반적인 오류를 자동 감지하고 해결 버튼 제공:

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️ Apache 시작 실패                                           │
│                                                                 │
│  원인: 포트 8080이 다른 프로그램에서 사용 중                    │
│  충돌 프로그램: java.exe (PID: 12345)                          │
│                                                                 │
│  [다른 포트로 변경 (8081)]  [프로세스 정보 보기]  [무시]       │
└─────────────────────────────────────────────────────────────────┘
```

**자동 수정 가능한 오류 (안전):**
| 오류 | 자동 수정 |
|------|-----------|
| 포트 충돌 | 빈 포트로 자동 변경 제안 |
| Node PATH 문제 | 환경 변수 재설정 |
| PHP 확장 누락 | 자동 활성화 |

**진단 + 가이드 제공 (고급):**
| 오류 | 대응 |
|------|------|
| MariaDB 실행 실패 | 원인 진단(권한/포트/손상) + 복구 가이드 |
| MariaDB data 손상 | ⚠️ 백업 확인 후 재초기화 (고급 옵션) |
| 충돌 프로세스 | 프로세스 정보 표시 (직접 종료는 고급) |

> **주의:** MariaDB data 폴더 재초기화는 데이터 유실 위험.
> 반드시 백업 확인 경고 후 "고급 옵션"에서만 실행.

> **프로세스 탐지 한계:** 충돌 프로세스는 프로세스명/PID까지만 확실히 표시.
> 서비스 분류(Apache/Next/Vite 등)는 추정일 수 있음.
> **충돌 판단 기준:** 포트 리스닝 소켓 점유(PID 기준) + 필요 시 프로세스 경로 표시

### 프로젝트 실행 프리셋
프레임워크별 기본 명령어 자동 설정:

| 프레임워크 | 시작 명령 | 빌드 명령 |
|------------|-----------|-----------|
| Next.js | `pnpm dev` | `pnpm build` |
| Vite (React/Vue) | `pnpm dev` | `pnpm build` |
| Laravel | `php artisan serve` | - |
| Django | `python manage.py runserver` | - |
| FastAPI | `uvicorn main:app --reload` | - |

> **의존성 설치 정책:** 프로젝트 생성 후 최초 실행 시,
> 의존성 설치(`pnpm install` / `composer install`)는 자동 수행(옵션) 또는 원클릭 버튼 제공.
> **실패 시:** 로그 링크 제공 + "재시도" 버튼 + 수동 명령 복사

### DB 연결 테스트
프로젝트 설정에서 원클릭 연결 테스트:

```
┌─────────────────────────────────────────────────────────────────┐
│  Database Connection                                            │
├─────────────────────────────────────────────────────────────────┤
│  Host: localhost     Port: 3306                                │
│  Database: my_app_db                                            │
│  User: my_app_user                                              │
│  Password: ********                                             │
│                                                                 │
│  [테스트 연결]  [연결 문자열 복사]                              │
│                                                                 │
│  ✅ 연결 성공! (응답시간: 12ms)                                 │
└─────────────────────────────────────────────────────────────────┘
```

**연결 문자열 복사 형식:**
- MySQL: `mysql://user:pass@localhost:3306/db`
- Laravel: `DB_CONNECTION=mysql` (전체 블록)
- Django: `DATABASE_URL=mysql://...`

### 포트 상태 대시보드 (앱 시작 시)
앱 시작 시 주요 포트(8080, 3306, 3000-3010) 점유 상태 스캔:

```
┌──────────────────────────────────────────────────────────────────┐
│  ⚠️ 포트 충돌 감지                                               │
│  3306: mysqld.exe (기존 MySQL)  |  8080: java.exe                │
│  [상세 보기]                                                     │
└──────────────────────────────────────────────────────────────────┘
```

- 대시보드 상단에 충돌 표시
- 충돌하는 포트/프로세스 목록
- [상세 보기] 클릭 시 해결 옵션 제시

### 브라우저 자동 열기
프로젝트 시작 성공 시 브라우저 자동 실행 옵션:

```
┌─────────────────────────────────────────────────────────────────┐
│  프로젝트 설정                                                  │
├─────────────────────────────────────────────────────────────────┤
│  ☑ 시작 성공 시 브라우저 자동 열기                              │
│     URL: http://localhost:3000                                 │
│     대기 시간: [3초 ▼] (서버 준비 대기)                         │
└─────────────────────────────────────────────────────────────────┘
```

- Health Check 통과 후 브라우저 실행
- 대기 시간 설정 가능 (기본 3초)
- 프로젝트별 개별 설정

### 포트 정책 (자동 할당 룰)

```typescript
interface PortPolicy {
  // 서비스 고정 포트
  apache: 8080;
  apacheSSL: 8443;
  mariadb: 3306;

  // 프로젝트 포트 범위
  projectRange: {
    start: 3000;
    end: 3999;
  };

  // 할당 규칙
  allocation: 'sequential';  // 3000, 3001, 3002...
  onConflict: 'increment';   // 충돌 시 +1
}
```

> **프레임워크 기본 포트 정책:**
> - **기본 동작:** 3000-3999 순차 할당
> - **프레임워크 감지 시 추천:**
>   | 프레임워크 | 기본 포트 | 정책 |
>   |------------|-----------|------|
>   | Next.js | 3000 | 정책 범위 내 ✅ |
>   | Vite | 5173 | 경고 라벨 + "3001로 변경" 원클릭 제공 |
>   | Django | 8000 | 경고 라벨 + "3002로 변경" 원클릭 제공 |
>   | FastAPI | 8000 | 경고 라벨 + "3003으로 변경" 원클릭 제공 |
> - **사용자 선택 우선:** 경고만 표시, 강제 변경 없음

**포트 범위 정책:**
| 범위 | 용도 | 충돌 시 | 비고 |
|------|------|---------|------|
| 8080 | Apache (HTTP) | 8081 제안 | 고정 (SSL은 Phase 2) |
| 3306 | MariaDB | 3307 제안 | 고정 |
| 3000-3999 | 프로젝트 (자동 할당) | +1 자동 | 권장 |
| 4000-4999 | 수동 설정 가능 | 경고만 | 향후 특수 서비스용 |
| 5000+ | 자유 사용 | 경고 없음 | 프레임워크 기본 등 |

**MariaDB 포트 충돌 처리:**
```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️ MariaDB 포트 충돌                                          │
│                                                                 │
│  포트 3306이 다른 프로세스에서 사용 중입니다.                   │
│  프로세스: mysqld.exe (기존 MySQL/MariaDB 설치)                 │
│                                                                 │
│  [포트 3307로 변경]  [기존 프로세스 정보 보기]  [취소]         │
└─────────────────────────────────────────────────────────────────┘
```

**포트 변경 정책:**
- 포트 변경은 **제안(Confirm)** 방식 - 사용자가 수락해야 적용
- 묻지 않고 자동 변경하지 않음
- 적용 시 설정 파일 동기화:
  - Apache: `httpd.conf`
  - MariaDB: `my.ini`
  - DevPort: `devport.json` (레지스트리)

**포트 변경 롤백 정책:**
1. 설정 파일 백업 (.bak)
2. 신규 설정 반영
3. 서비스 재시작 + Health Check
4. **실패 시 자동 롤백** (백업 파일 복원)

### 업데이트 정책

```typescript
interface UpdatePolicy {
  // DevPort 앱 업데이트
  app: {
    checkOnStartup: true;
    autoDownload: false;      // 수동 다운로드
    channel: 'stable';        // stable | beta
  };

  // 런타임 업데이트 (향후)
  runtime: {
    autoUpdate: false;        // MVP에서는 수동
    notifyNewVersion: true;   // 새 버전 알림
  };
}
```

**업데이트 범위:**
| 구분 | MVP 지원 | 향후 지원 |
|------|----------|-----------|
| DevPort 앱 | 수동 다운로드 | 인앱 업데이트 |
| Node.js | 재설치 필요 | 인앱 버전 전환 |
| PHP | 재설치 필요 | 인앱 버전 전환 |
| phpMyAdmin | 재설치 필요 | 자동 업데이트 |

---

## 검증 계획

### 1. 서비스 관리 테스트
- [ ] Apache 시작 → http://localhost:8080 접속 확인
- [ ] Apache 중지 → 접속 불가 확인
- [ ] MariaDB 시작 → `mysql -u root` 연결 확인
- [ ] phpMyAdmin 접근 → http://localhost:8080/phpmyadmin 확인
- [ ] 서비스 크래시 시뮬레이션 → 자동 재시작 확인
- [ ] Health Check → 4단계 상태 전환 확인

### 2. 로그 시스템 테스트
- [ ] 서비스 시작 시 로그 파일 생성 확인
- [ ] 실시간 로그 스트리밍 (UI에서 업데이트)
- [ ] 로그 로테이션 (50MB 초과 시)
- [ ] 30일 이상 로그 자동 삭제

### 3. 프로젝트 생성 테스트
- [ ] Next.js 프로젝트 생성 → 개발서버 시작
- [ ] Laravel 프로젝트 생성 → `php artisan serve` 확인
- [ ] Django 프로젝트 생성 → `runserver` 확인
- [ ] 포트 충돌 감지 → 다른 포트 제안

### 4. DB 자동화 테스트
- [ ] 프로젝트 생성 시 DB 자동 생성 확인
- [ ] 전용 사용자 생성 및 권한 확인
- [ ] .env에 DATABASE_URL 자동 설정 확인
- [ ] 덤프 → 복원 → 데이터 무결성 확인
- [ ] 비밀번호 재생성 기능
- [ ] **MariaDB 연결 검증:** DevPort DB 연결 테스트 및 프로젝트 전용 계정으로 수행 (root 노출 없음)

### 5. 보안 테스트
- [ ] MariaDB: 외부 IP에서 접속 시도 → 차단 확인
- [ ] phpMyAdmin: 외부 IP에서 접속 시도 → `Require local`로 차단 확인
- [ ] Apache: 8080 포트 바인딩 확인

### 6. 환경변수 테스트
- [ ] .env 편집 → 저장 → 파일 반영 확인
- [ ] 프로필 전환 (dev → staging) → 값 변경 확인
- [ ] 민감 값 마스킹 표시 확인

### 7. hosts 파일 테스트
- [ ] 도메인 등록 → hosts 파일에 추가 확인
- [ ] 도메인 삭제 → hosts 파일에서 제거 확인
- [ ] 관리자 권한 UAC 프롬프트 확인

### 8. 오류 복구 테스트
- [ ] Apache 포트 충돌 → 8081 자동 제안
- [ ] MariaDB 포트 충돌 → 3307 자동 제안
- [ ] MariaDB 실행 실패 → 진단 + 복구 가이드 표시
- [ ] MariaDB data 재초기화 → 백업 경고 + 고급 옵션 확인
- [ ] Python 미설치 시 → 설치 안내 표시

### 9. UX 기능 테스트
- [ ] DB 연결 테스트 버튼 → 연결 성공/실패 표시
- [ ] 연결 문자열 복사 → 클립보드 복사 확인
- [ ] 프로젝트 실행 프리셋 → 자동 명령 설정 확인
- [ ] 포트 자동 할당 → 3000부터 순차 할당 확인

### 10. 창/트레이 동작 테스트
- [ ] 창 닫기(X) → 트레이로 최소화 (서비스 유지)
- [ ] 트레이 아이콘 더블클릭 → 창 복원
- [ ] 트레이 메뉴 "종료" → 모든 서비스 종료 + 앱 종료
- [ ] Task Scheduler 자동 시작 → 트레이 상태로 시작

### 11. 언인스톨 테스트
- [ ] 기본 삭제 → 앱+런타임만 삭제, projects/backups 유지
- [ ] 전체 데이터 삭제 → projects/backups/data 삭제 확인
- [ ] 시스템 원복 → hosts/방화벽/스케줄러 제거 확인
- [ ] 삭제 전 프로세스 종료 확인
- [ ] 파일 잠김 시 재부팅 예약 옵션 동작

---

## 변경 이력

### v3.2 업데이트 (정합성 완성 및 UX 강화)

**정합성 해결:**
1. DB 정책 통일 - 자동 생성이 MVP 기본 (XAMPP 대비 차별점)
2. phpMyAdmin 외부 접근 보안 경고 강화

**MVP 기능 확장:**
3. Apache Reverse Proxy MVP 포함 - http://my-app.test (포트 없이) 접속 지원
4. 지원 프레임워크: Next.js, Vite, Svelte

**프로세스 안전성:**
5. Job Object 강조 - DevPort 시작 프로세스만 종료 (다른 프로세스 보호)
6. 포트 Kill 범위 명시 - 해당 프로젝트 포트에 한정

**UX 개선:**
7. 포트 충돌 대시보드 (앱 시작 시 스캔)
8. 브라우저 자동 열기 옵션
9. DPAPI 복호화 실패 복구 절차 상세화
10. 프레임워크 기본 포트 정책 표 추가

**향후 계획:**
11. PHP FastCGI 전환 검토 (Phase 2+)
12. SSL(443) Reverse Proxy (Phase 2)

### v3.1 업데이트 (운영 안정성 강화)

**DB 정책 변경:**
- `devport_internal` 내부 계정 제거 → 사용자가 phpMyAdmin에서 직접 관리
- root 비밀번호: 사용자 입력, DPAPI 암호화 저장
- 암호화 키 재설정 가이드 추가

**프로세스 종료 강화:**
- Python: CTRL+C 시뮬레이션 → fallback taskkill /T
- 강제 포트 해제: Job Object 실패 시 포트 점유 PID 역추적 Kill
- Phase 1 코어 기능에 포함

**DB 복원 UX:**
- 대용량 복원 시 프로그레스 바 (바이트 단위 진행률)
- 별도 스레드 실행으로 UI 멈춤 방지

**방화벽 정책:**
- runtime 내 주요 바이너리 일괄 등록 (httpd, mysqld, node, php)

**MariaDB 초기화 실패:**
- 3가지 케이스 분기 UI (포트 충돌/권한/기존 MySQL)

**hosts 충돌:**
- 덮어쓰기 금지 기본, 새 도메인 추천 제공

**신규 섹션:**
- DevPort Config Schema (schemaVersion: 1)
- Crash Recovery 정책 (PID stale cleanup, 포트 재스캔)
- 개인정보 보호 (Telemetry 없음 선언)

### v3.0.1 업데이트 (MVP 최적화)

**MVP 조정:**
1. **Apache SSL 제거** - 8080만 제공, SSL(8443)은 Phase 2 이후
2. **root → internal 계정** - `devport_internal` 내부 계정으로 변경
3. **Node.js 종료** - Job Object 기반 (stopCommand 불필요)

### v3.0 업데이트 (구현 리스크 완전 해결)

**Critical Issues 해결:**
1. MariaDB 외부 접근 정책 통일 (bind-address, IP allowlist)
2. phpMyAdmin 접근 제한 정책 명확화 (기본/외부 Apache 설정)
3. Health Check 타이밍 조정 (interval 5s, timeout 2s, retries 2)

**High Priority 해결:**
4. Stop 정책 타임아웃 명확화 (stopCommand 10s → processTree 5s)
5. 백업 파일명 타임스탬프 포함 (HHmmss)
6. Python Store stub 판별 로직 구체화

**Medium Priority 해결:**
7. stopCommand 타입 명세 (string | object)
8. 로그 로테이션 daily 동작 명시
9. 포트 범위 정책 명확화 (3000-3999 권장, 4000+ 자유)
10. installCommand 필드 추가

**기타 보강:**
- Python 용어: "번들 제외(연동 지원)"
- Node/PHP 시스템 버전: MVP는 감지/표시만
- 설치 경로: `C:\DevPort\` 고정 (MVP)
- 방화벽: 2단계 정책 명확화

### v2.9.5 업데이트 (언인스톨 핵심 원칙 보강)

**언인스톨 핵심 원칙 3줄 추가:**
1. 번들은 `C:\DevPort\` 하위에만 배치 → 폴더 삭제로 완전 제거
2. 시스템 추가 항목은 `DevPort-*` 마킹 → Uninstall C에서 원복
3. `%LOCALAPPDATA%\DevPort` 설정/캐시 → 모드별 삭제 범위 명시

**정합성 수정:**
- hosts 마커 통일: `# DevPort BEGIN` ~ `# DevPort END`
- Uninstall 모드에 `%LOCALAPPDATA%\DevPort` 경로 추가

### v2.9.4 업데이트 (언인스톨 정책 추가)

**언인스톨 정책 (섹션 9 추가):**
- 3단계 Uninstall 모드 (기본/전체 데이터/시스템 원복)
- 시스템 변경 마킹 및 추적 제거 정책
- 안전한 삭제를 위한 5가지 설계 원칙
- Uninstall UI 예시

**MVP Phase 5 업데이트:**
- 언인스톨러 작업 항목 추가

### v2.9.3 업데이트 (최종 완성)

**문서 패치 5개:**
1. **프로젝트 Health Check** - 기본 TCP + 선택 HTTP `/health` 엔드포인트
2. **Projects Stop fallback** - stopCommand → 트리 종료 → 포트 PID 종료 순서
3. **hosts 편집 안정성** - DevPort 전용 marker 구간 + 멱등 적용
4. **외부 DB 접근 계정** - `user@'%'` 금지 → `user@'<ip>'` 형태로 제한
5. **MariaDB 연결 검증** - 프로젝트 전용 계정으로 수행 (root 노출 없음)

**차별화 포인트 추가:**
- 런타임 스냅샷, 실행 레시피, 백업 사전 점검, 에러 하이라이트, 충돌 매트릭스, JSON 스키마 버전

### v2.9.2 업데이트 (구현 리스크 제거)

**필수 리스크 해결 7개:**
1. **envProfile Phase 4 통일** - `development/staging/production`
2. **phpMyAdmin Tool 분류** - 독립 프로세스 아님, `dependsOn: apache`
3. **MariaDB SELECT 1 주체** - DevPort 내부 자동화 계정(root)으로만 실행
4. **포트 변경 롤백 정책** - 백업 → 적용 → 헬스체크 → 실패 시 자동 롤백
5. **프로젝트 포트 범위** - 기본 3000-3999, 고급에서 5173 등 허용, 경고 라벨
6. **DB 덤프/복원 옵션** - routines/triggers/single-transaction, utf8mb4, 진행률 표시
7. **외부 접근 allowlist** - 고급 메뉴에서만 + IP 제한 동반

### v2.9.1 업데이트 (최종 정합성)

**세부 정책 명확화:**
1. **Health Check 엔드포인트** - Apache/phpMyAdmin/MariaDB 각각 명시
2. **Stop 타임아웃** - stopCommand 최대 10초 대기 후 강제 종료
3. **방화벽 정책 통일** - 기본=없음, 외부 접근 허용 시점에만 안내
4. **envProfile 통일** - `development | staging | production`

### v2.9 업데이트 (구현 준비 완료)

**필수 수정 4개:**
1. **도메인 TLD 변경** - `.local` → `.test` (mDNS 충돌 방지, RFC 6761 예약)
2. **원클릭 열기 URL** - 포트 포함으로 수정 (`http://my-app.test:3000`)
3. **Health Check 기본값** - interval 3000ms, timeout 1000ms, retries 3
4. **AutoRestart 제한** - 최대 5회, 초과 시 Error 고정 + 수동 개입

**운영 안정성 보강 6개:**
5. **의존성 설치 실패** - 로그 링크 + 재시도 버튼 + 수동 명령 복사
6. **MariaDB 설치/복구** - 손상 감지 시 재초기화 옵션 + 자동 백업 시도
7. **포트 충돌 판단** - 소켓 점유(PID) + 프로세스 경로 표시
8. **방화벽 정책** - 외부 접근 허용 옵션 켤 때만 안내
9. **LICENSES 경로** - `C:\DevPort\LICENSES\` 명시
10. **Apache vhost 용도** - 내부 도구 노출용으로 스코프 명확화

**문구/구조 다듬기:**
- envProfile: 로컬 저장 프로필임을 명시
- 백업 파일명: `YYYY-MM-DD_dbname.sql` 템플릿화

### v2.8 업데이트 (운영 리스크 최소화)

**필수급 보강 5개:**
1. **도메인 접속 예시** - `http://my-app.test:3000` (포트 포함) 명시
2. **root 암호화 키 관리** - DPAPI 기반, 머신 바인딩, 이동 시 재연결 필요
3. **phpMyAdmin 디렉터리 권한** - Users 읽기 권한, Apache 접근 가능
4. **Health Check 전환 규칙** - N회 실패=Unhealthy, 프로세스 종료=Error
5. **의존성 설치 정책** - 최초 실행 시 자동 수행(옵션) 또는 원클릭

**정합성 보강 5개:**
6. **Stop 정책 분리** - Services vs Projects 구분
7. **가상호스트 스코프** - MVP: phpMyAdmin용, 프로젝트 프록시는 향후
8. **방화벽 대상 명시** - DevPortManager.exe 또는 httpd.exe
9. **백업 실패 처리** - 시작은 계속 진행(경고만), 타임아웃 시 백그라운드
10. **라이선스 문구** - LICENSES/NOTICE 파일 포함

### v2.7.1 업데이트 (PRD 최종 완성)

**필수급 보강 4개:**
1. **Quit 예외 정책** - "서비스 유지하고 앱만 종료"는 고급 옵션(향후)
2. **root 비밀번호 저장/복구** - 로컬 암호화 저장, 복구 옵션(재초기화/재연결)
3. **phpMyAdmin 외부 접근** - 기본 미지원, 고급 메뉴에서만 + 보안 경고
4. **Stop 정책** - 프로세스 트리 종료(부모+자식), stopCommand 우선 적용

**권장 보강 4개:**
5. **Python Store stub 판별** - Microsoft Store alias 감지 → 실제 경로 우선
6. **포트 정책 일관화** - 3000-3999 기본, Vite 5173 등은 안내/변경 제안
7. **도메인-Apache 관계** - hosts만 매핑 기본, reverse proxy는 고급(향후)
8. **백업 심볼릭 링크 대체** - Windows 호환 위해 latest.txt 포인터 파일 사용

**문구 다듬기:**
- 방화벽: "첫 실행 또는 포트 변경 시" 트리거 명시
- MariaDB 로그: "general/error 등 설정에 따라" 완화

### v2.7 업데이트 (정합성 강화 및 초보자 가이드)

**변경 사항:**
1. **hosts 파일 수정 시점 변경**
   - 설치 시 → 도메인 등록/삭제 시점에 UAC 권한 요청
   - 설치 시 불필요한 관리자 권한 작업 제거

2. **방화벽 정책 명확화**
   - "localhost 포트 허용" → "필요 시 방화벽 예외(프로그램 단위) 안내/자동 설정(옵션)"
   - 보안은 127.0.0.1 바인딩으로 보장

3. **Services 패널에서 Redis 제거**
   - MVP에 미포함이므로 예시에서 제거
   - 향후 플러그인 방식 안내 추가

4. **설치 후 빠른 확인(1분) 섹션 추가**
   - Apache 시작 확인
   - phpMyAdmin 접속 확인
   - 프로젝트 생성/실행 확인

5. **프로세스 탐지 한계 명시**
   - 프로세스명/PID까지만 확실히 표시
   - 서비스 분류는 추정일 수 있음

### v2.6 업데이트 (정책 명확화 및 구현 가이드)

**변경 사항:**
1. **운영 모드 표 수정**
   - 종료 방식: "Quit(앱 종료) 시 모든 서비스 함께 종료 (X=트레이 최소화는 서비스 유지)"

2. **Task Scheduler 자동 시작 정책 추가**
   - Quit 시 스케줄러 설정 유지 (다음 부팅에 다시 자동 시작)
   - 완전 해제는 설정 메뉴에서만 가능
   - 사용자 안내 문구 추가

3. **cookie 인증 설명 추가**
   - phpMyAdmin 로그인 방식 상세 설명
   - 비밀번호 없는 계정 차단 명시

4. **포트 변경 정책 명확화**
   - 포트 변경은 제안(Confirm) 방식
   - 묻지 않고 자동 변경하지 않음
   - 설정 파일 동기화 목록

5. **PATH 관리 정책 추가**
   - 시스템 환경변수 자동 변경하지 않음
   - DevPort 프로세스 실행 시 자체 PATH 주입
   - 시스템 PATH 변경은 고급 옵션으로만

6. **Redis 지원 정책 추가**
   - MVP에서는 미포함
   - 향후 플러그인 방식으로 제공

### v2.5 업데이트 (정합성 및 안전성 개선)

**변경 사항:**
1. **창 닫기 vs 종료 UX 정책 추가**
   - 창 닫기(X): 트레이로 최소화 (서비스 유지)
   - 종료(Quit): 모든 서비스 함께 종료
   - Task Scheduler 자동 시작 시 트레이 상태로 시작

2. **root 계정 정책 명확화**
   - root는 DevPort 내부 자동화에만 사용
   - 사용자는 root 비밀번호 직접 입력 불필요
   - phpMyAdmin 로그인은 프로젝트 전용 계정으로 안내
   - .env 예시를 프로젝트 계정 기준으로 수정

3. **오류 복구 안전성 강화**
   - MariaDB data 재초기화 → 고급 옵션 + 백업 경고
   - "프로세스 종료" → "프로세스 정보 보기"로 완화
   - 자동 수정 vs 진단+가이드 분리

4. **포트 충돌 처리 개선**
   - MariaDB 3306 충돌 시 3307 자동 제안
   - Apache 8080 충돌 시 8081 자동 제안

### v2.4 업데이트 (UX 개선 및 정책 명확화)

**변경 사항:**
1. **Windows Service 제거** - 프로세스 방식만 지원
   - Windows 서비스 등록 옵션 제거
   - 자동 시작은 Task Scheduler로만

2. **phpMyAdmin 보안 강화**
   - `AllowNoPassword = false` (비밀번호 필수)
   - root 비밀번호 설치 시 필수 설정
   - 프로젝트 전용 계정으로 로그인 유도

3. **UX 개선 섹션 추가**
   - 오류 복구 가이드 (원클릭 자동 수정)
   - 프로젝트 실행 프리셋 (자동 명령 설정)
   - DB 연결 테스트 버튼 + 연결 문자열 복사
   - 포트 정책 명시 (3000부터 순차, 충돌 시 +1)
   - 업데이트 정책 (앱 vs 런타임 분리)

4. **Python 관련 불일치 수정**
   - 버전 관리 섹션에서 Python 번들 언급 제거
   - Phase 5에서 Python 번들링 제거

### v2.3 업데이트 (Python 번들 제외)

**변경 사항:**
1. **Python 번들 제외** - 사용자가 직접 Python 설치
   - Python 프로젝트(Django, Flask, FastAPI) 지원은 유지
   - 시스템 Python 자동 감지
   - Python 미설치 시 안내 메시지 표시
2. **번들 크기** - ~410MB → ~380MB로 축소

### v2.2 업데이트 (사용자 피드백 반영)

**변경 사항:**
1. **운영 모드** - 포터블 → 설치형으로 변경 (NSIS 인스톨러)
2. **Apache 포트** - 80/443 → 8080으로 변경 (충돌 방지)
3. **phpMyAdmin** - 별도 서비스 → Apache 서브디렉토리로 변경
   - `http://localhost:8080/phpmyadmin` 형식으로 접근
   - Apache Alias 설정으로 연동
4. **랜덤 포트 제거** - phpMyAdmin 고정 경로 사용

### v2.1 업데이트 (리뷰 피드백 반영)

**추가된 섹션:**
1. **운영 모드** - 설치형 방식으로 권한 문제 해결
2. **Python 임베디드 제한 사항** - 한계 및 대응 방안
3. **상태 모델 (Health Check)** - 4단계 상태 (running/stopped/error/unhealthy)
4. **로그 관리 정책** - 로테이션, 보관 기간
5. **DB 보안 정책** - localhost 바인딩, 프로젝트별 계정
6. **DB 계정 자동화** - 프로젝트 생성 시 자동 DB/사용자 생성
7. **버전 관리** - MVP 단일 버전, 향후 다중 버전 지원 계획

**수정된 섹션:**
- **핵심 기능 요약** - 보안 정책 항목 추가
- **phpMyAdmin** - Apache 서브디렉토리로 통합
- **Project 인터페이스** - 확장된 필드 (database, runtimeVersions 등)
- **Service 인터페이스** - 새로 추가 (healthCheck, logConfig 등)
- **MVP 구현 순서** - 5단계로 재구성, 우선순위 명확화
- **검증 계획** - 보안 테스트, 로그 테스트 등 추가